import { StudioPage } from '@features/studio';
import { studioMetadata } from '../../metadata';

export const metadata = studioMetadata;

export default function StudioProjectDesignRoutePage() {
  return <StudioPage />;
}
